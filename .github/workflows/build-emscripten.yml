name: Emscripten build

on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        os:         [windows-2025, ubuntu-24.04, macos-26]
        build_type: [Debug, Release]

        exclude:
          # Exclude MacOS-Release
          - os:         macos-26
            build_type: Release

        include:
          - os:        windows-2025
            host_name: Win10

          - os:        ubuntu-24.04
            host_name: Linux

          - os:        macos-26
            host_name: MacOS

    runs-on: ${{ matrix.os }}
    name:    ${{ matrix.host_name }} -> Emscripten, ${{ matrix.build_type }}

    steps:
    - name: Checkout
      uses: DiligentGraphics/github-action/checkout@v26

    - name: Set up build environment
      if:   success()
      uses: DiligentGraphics/github-action/setup-build-env@v26
      with:
        platform: Web

    - name: Configure CMake
      if:   success()
      uses: DiligentGraphics/github-action/configure-cmake@v26
      with:
        build-type: ${{ matrix.build_type }}
        cmake-args: "-DDILIGENT_BUILD_TOOLS_INCLUDE_TEST=ON"

    - name: Build
      if:   success()
      uses: DiligentGraphics/github-action/build@v26
